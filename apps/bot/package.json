{
    "name": "bot",
    "version": "0.0.1",
    "private": true,
    "scripts": {
        "dev": "bun run src/index.ts",
        "build": "rm -rf dist && bun run src/build.ts",
        "start": "bun run dist/index.js"
    },
    "dependencies": {
        "seyfert": "2.2.0"
    },
    "devDependencies": {
        "tsconfig": "workspace:*"
    }
}
