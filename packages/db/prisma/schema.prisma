// This is your Prisma schema file,
// learn more about it in the docs: https://pris.ly/d/prisma-schema

generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "sqlite"
  url      = env("DATABASE_URL")
}

model User {
  id        String    @id @unique
  createdAt DateTime  @default(now())
  username  String
  model     String
  chats     Chat[]
  messages  Message[]
}

model Chat {
  id        String    @id @unique @default(uuid())
  createdAt DateTime  @default(now())
  authorId  String?
  messages  Message[] @relation(name: "MessageToChat")
  author    User?     @relation(fields: [authorId], references: [id])
}

model Message {
  id        String   @id @unique @default(uuid())
  chatId    String
  createdAt DateTime @default(now())
  authorId  String
  content   String
  role      String
  chat      Chat     @relation(name: "MessageToChat", fields: [chatId], references: [id])
  author    User     @relation(fields: [authorId], references: [id])
}
